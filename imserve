#! python3
# Usage: Run this script from the directory containing your images/videos.
# It will start a web server on port 8080 (or next available port).
#
# If an mp4 fails to play in the browser (e.g. due to H.265/HEVC or unusual
# audio codecs), you can open it directly in mpv which supports all codecs:
#   mpv --keep-open http://localhost:8080/path/to/video.mp4
import os
from flask import Flask, send_from_directory
import time
from pathlib import Path
import subprocess


app = Flask(__name__)

@app.after_request
def add_headers(response):
    response.headers['Accept-Ranges'] = 'bytes'
    response.headers['Access-Control-Allow-Origin'] = '*'
    response.headers['Access-Control-Allow-Methods'] = 'GET, HEAD, OPTIONS'
    response.headers['Access-Control-Allow-Headers'] = 'Range'
    response.headers['Access-Control-Expose-Headers'] = 'Content-Length, Content-Range, Accept-Ranges'
    return response
script = """ let selected = null;
let marked_inds = [];
function select(ind){
    if (ind < 0) {
        ind = 0
    }
    if (ind >= paths.length) {
        ind = paths.length - 1
    }
    if (marked_inds.includes(selected)) {
        $("#item"+selected).css('background','#03dac6')
    } else {
        $("#item"+selected).css('background','#1e1e2e')
    }
    $("#item"+ind).css('background','#2196f3')
    selected=ind
    // $("#display").attr('src',paths[ind])
    // detect mp4
    if (paths[ind].match('\.mp4')) {
        $("#display_inner").html('<video width="100%" controls><source id="display_el" src="" type="video/mp4"></video>')
    } else {
        $("#display_inner").html('<img width="100%" id="display_el" src=""/>')
    }
    let view_url = new URL("http://"+location.host+paths[ind])
    view_url.searchParams.set('ts',Date.now())
    $("#display_el").attr('src',view_url.href)
    
    // window.location.hash=ind
    let url = new URL(window.location)
    url.hash = '#'+ind
    console.log(url.href)
    history.replaceState(undefined, undefined,url.href)
    let pageTop = $(window).scrollTop();
    let pageBottom = pageTop + window.innerHeight;
    let elementTop = $("#item"+ind).offset().top;
    let elementBottom = elementTop + $("#item"+ind).height();
    if (elementTop < pageTop) {
       $(window).scrollTop(elementTop)
    } else if (elementBottom > pageBottom) {
       $(window).scrollTop(elementBottom-window.innerHeight)
    }
}
// add the selected file to the list of marked files
function mark(ind){
    $("#item"+ind).css('background','#03dac6')
    marked_inds.push(ind)
}

// remove the selected file to the list of marked files
function unmark(ind){
    $("#item"+ind).css('background','#1e1e2e')
    marked_inds = marked_inds.filter(function(value, index, arr){ return (value != ind)})
}

$(document).ready(function(){
    let hash = window.location.hash.substr(1)
    if (hash.length > 0) {
        select(parseInt(window.location.hash.substr(1)))
    }
    window
    let url = new URL(document.location.href)
    let width = url.searchParams.get('w')
    if (width) {
        $("#display").width(width)
    }
    $( "#display" ).resizable({
        resize: function( event, ui ) {
        let url = new URL(document.location.href)
        url.searchParams.set("w",ui.size.width)
        history.replaceState({ additionalInformation: 'Updated the URL with JS' }, "imserve", url);
        }
    });
})
// print an array with the file names of all marked files
function print_marked(){
    console.log(marked_inds.map(function(ind){return paths[ind]}))
}

document.onkeydown = function(e) {
    switch(e.which) {
        case 37: // left
        unmark(selected)
        break;

        case 38: // up
        select(selected-1)
        break;

        case 39: // right
        mark(selected)
        break;

        case 40: // down
        select(selected+1)
        break;
        default: return; // exit this handler for other keys
    }
    e.preventDefault(); // prevent the default action (scroll / move caret)
};
"""

image_extensions = ['png','jpg','jpeg','gif']
from flask import request

@app.route('/')
@app.route("/<path:subpath>")
def hello_world(subpath=''):
    path = subpath
    print('path: ',path,os.path.isdir('./'+path))
    ts = time.time()
    if path is None or os.path.isdir('./'+path):
        if path is None:
            path = ""
    
        # self.send_response(200)
        # self.send_header("Content-type", "text/html")
        # self.end_headers()
        style = '''
                * { box-sizing: border-box; }
                body { margin: 0; background: #121212; color: #e0e0e0; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; }
                #page { display: flex; flex-direction: column; height: 100vh; }
                #main { display: flex; overflow-y: auto; flex: 1; }
                #list { display: flex; overflow-y: scroll; flex-shrink: 0; flex-direction: column; background: #1e1e2e; min-width: 250px; }
                #list .item { border-bottom: 1px solid #2a2a3e; transition: background 0.15s ease; cursor: pointer; }
                #list .item:hover { background: #2a2a3e !important; }
                h3 { flex-grow: 0; padding: 12px 16px; margin: 0; background: #16162a; border-bottom: 1px solid #2a2a3e; font-size: 14px; font-weight: 500; }
                a { color: #64b5f6; text-decoration: none; }
                a:hover { text-decoration: underline; color: #90caf9; }
                #display_inner { padding: 16px; }
                #display_inner img, #display_inner video { border-radius: 4px; }
                ::-webkit-scrollbar { width: 8px; }
                ::-webkit-scrollbar-track { background: #1e1e2e; }
                ::-webkit-scrollbar-thumb { background: #3a3a5e; border-radius: 4px; }
                ::-webkit-scrollbar-thumb:hover { background: #4a4a6e; }
        '''
        res = f'''<html>
        <head>
        <script src=\"https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js\"></script>
<script src="https://code.jquery.com/ui/1.13.2/jquery-ui.js"></script>
<link rel="stylesheet" href="//code.jquery.com/ui/1.13.2/themes/base/jquery-ui.css">
        <script>{script}</script><title>{path}</title>
        <style>
        {style}
        </style>

        </head>
        '''
        # files = sorted(os.listdir('./'+path))

        # find files where all users have read permissions
        cmd = 'find ./{} -maxdepth 1 -perm -444 -printf "%P\\n"'.format(path).split()
        result = subprocess.run(cmd, stdout=subprocess.PIPE).stdout.decode('utf-8')
        files = [x[2:] for x in result.split('\n')]
        # remove hidden and blank files
        files = sorted([x for x in files if len(x)>0 and x[0] != '.'])
        

        if len(path) == 0:
            root_path = ''
        else:
            root_path = f'/{path}'
        res += '<body><div id="page">'

        # header
        mount_path = os.getcwd()
        parts = Path(path).parts
        header_links = f'<a href="/">{mount_path}/</a>'
        for i in range(len(parts)): 
            sub_path = os.path.join(*parts[:i+1])
            name = parts[i]
            header_links +=f'<a href="/{sub_path}">{name}/</a>'
        res += f'<h3>{header_links}</h3>'

        res += f'<div id="main">'
        res += '<div id="list">'
        for i,fil in enumerate(files):
            print(root_path,fil)
            onclick =f"onclick=\"select({i})\""
            res += f'<p class="item" {onclick} style="background: #1e1e2e; margin: 0; padding: 8px 16px" id="item{i}"><a href="{root_path}/{fil}">{fil}</a></p>'
        res += '</div>'
        # res += f'<div><img id="display" style="position: fixed" src=""/></div>'
        res += f'<div><div id="display" style="position: fixed; padding: 0.5em"><div id="display_inner"></div></div></div>'
        res += '</div>'
        paths = ','.join([f'"{root_path}/{fil}?ts={ts}"' for fil in files])
        end_script = f"let paths = [{paths}]"
        res += f"<script>{end_script}</script></div></body></html>"
        return res
    else:
        print(os.path.dirname(path),os.path.basename(path))
        return send_from_directory(os.getcwd()+'/'+os.path.dirname(path),os.path.basename(path))

if __name__ == '__main__':
    port = 8080
    for _ in range(100):
        try:
            # app.debug=True
            app.run(host='0.0.0.0', port=port,threaded=True)
            break
        except (OSError, SystemExit) as e:
            if isinstance(e, SystemExit) or e.errno == 98:
                port += 1
            else:
                raise
